from setuptools import setup

setup(
    name="debug_pytest",
    description="Exercício de debug com pytest",
    install_requires=[],
    setup_requires=["pytest-runner"],
    tests_require=["pytest"]
)
